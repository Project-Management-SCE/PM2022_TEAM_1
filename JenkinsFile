
pipeline {
    agent none

        stage(' Unit Tests') {
            agent {
                docker {
                    image 'salemgu/django_todo'
                }
            }
            steps {
		            withEnv(["HOME=${env.WORKSPACE}"]) {
                sh 'python manage.py test --tag=unit_test'
		      }
			}
        }

}
